# OS
Thumbs.db
.DS_Store

# Build/temp
*.log
*.bak
*.tmp

# Editor
.vscode/
.idea/

# Local secrets — never commit. Mirrors live in $HOME\.gcc-secrets\
# (regenerated by sync-from-gsm.py from Google Secret Manager).
.env
.env.*
!.env.example

# Workspace-scoped Cursor MCP — local to each developer (paths embed user
# profile, secret-key location). .cursor/rules/ DOES ship with the repo
# (shared house knowledge for the AI).
.cursor/mcp.json
.cursor/settings.json

# web.config is now tracked — it holds the IIS rewrite rules (admin
# console proxy + api host reverse-proxy + HTTPS redirect via CF-Visitor)
# that have to deploy with the site or routing breaks.
