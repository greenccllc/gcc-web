// GCC Proposal component library — public barrel.
// Re-exports every component + shared type from components.tsx.

export * from "./components";
export { default as tokens, color, font, size, line, track, space, rule, panel } from "../../tokens/tokens";
