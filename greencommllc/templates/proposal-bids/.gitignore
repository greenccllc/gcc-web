# Word/Office temp files
~$*
*.tmp

# Backup files (rely on git history instead)
*.bak
*.bak.*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor caches
.vscode/
.idea/

# Local docx2pdf scratch
*.~lock.*

# Optional: uncomment if rendered PDFs become a churn problem.
# Currently the .pdf is the cached preview that the merge pipeline
# reads, so it's tracked.
# *.pdf
