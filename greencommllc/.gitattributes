# Auto detect text files and perform LF normalization
* text=auto

# Office and image files are binary — no merge, no LF/CRLF normalization.
*.docx  binary
*.xlsx  binary
*.pptx  binary
*.pdf   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.zip   binary
